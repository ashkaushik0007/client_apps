import { ChangeDetectorRef, ElementRef, OnInit } from '@angular/core';
export declare class GtColumnWidthDirective implements OnInit {
    private hostElement;
    private cdRef;
    objectKey: string;
    widths: Object;
    constructor(hostElement: ElementRef, cdRef: ChangeDetectorRef);
    ngOnInit(): void;
    onResize($event: any): void;
    checkSize(): void;
}
